{
  "name": "opencart.code-workspace",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:main": "playwright test --grep @main",
    "test:sanity": "playwright test --grep @sanity",
    "test:regression": "playwright test --grep @regression",
    "test:datadriven": "playwright test --grep @datadriven",
    "test:main:headed": "playwright test --grep @main --headed",
    "test:main:end-to-end": "playwright test --grep @end-to-end --headed",
    "test:sanity:debug": "playwright test --grep @sanity --debug"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^25.6.0",
    "allure-playwright": "^3.7.1"
  },
  "dependencies": {
    "@faker-js/faker": "^10.4.0",
    "csv-parse": "^6.2.1"
  }
}
